<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    android:layout_marginStart="5dp"
    android:maxWidth="275dp"
    android:background="@drawable/drawable_received"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/appearReceived"
        android:maxWidth="230dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:text="Received text appears here"
        android:textSize="17.5sp"
        android:textColor="@color/black"
        android:padding="5sp"
        android:layout_marginStart="5dp"
        android:background="@drawable/drawable_received"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/appearReceivedTimestamp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/appearReceived"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="00:00"
        android:textSize="12sp"
        android:padding="5sp"/>

</androidx.constraintlayout.widget.ConstraintLayout>